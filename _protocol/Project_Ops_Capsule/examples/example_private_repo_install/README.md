# Example Private Repo Install

This example shows the minimal Project Ops Capsule v0.2 install surface for a private software project.

Every run records evidence; only signable runs update accepted truth.

